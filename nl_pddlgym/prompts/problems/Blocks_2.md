## Problem description

There are 5 given blocks (a,b,c,d,e) on the table and one available robot (robot). The blocks are stacked on top of each other in the following order (starting from the bottom): e,d,c,b,a. The goal is to stack the blocks in the following order (starting from the bottom): a,b,c,d,e.
