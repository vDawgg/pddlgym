## Problem description

There are 4 given blocks (a,b,c,d) on the table and one available robot (robot). All blocks are on the table and none of the blocks are stacked on top of each other. The goal is to stack the blocks in the following order (starting from the bottom): a,b,c,d.
