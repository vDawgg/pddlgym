## Problem description

There are 5 given blocks (a,b,c,d,e) on the table and one available robot (robot). a,b,c and e are on the table and d is stacked on e. The goal is to stack the blocks in the following order (starting from the bottom): a,b,c and a separate stack with e on d.
